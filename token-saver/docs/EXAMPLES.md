# Token-Saver 使用示例

本文档提供 Token-Saver Skill 的实际使用示例。

---

## 📋 目录

1. [基础用法](#基础用法)
2. [输入优化示例](#输入优化示例)
3. [上下文管理示例](#上下文管理示例)
4. [输出控制示例](#输出控制示例)
5. [文件读取优化示例](#文件读取优化示例)
6. [模式检测与脚本生成示例](#模式检测与脚本生成示例) ⭐
7. [综合示例](#综合示例)

---

## 基础用法

### 启用 Skill

Token-Saver 会在对话中自动工作，无需手动启用。

### 查看当前配置

```
用户："显示 token-saver 配置"
```

AI 会返回当前的所有配置项及其值。

---

## 输入优化示例

### 示例 1：简化冗长请求

**原始输入**：
```
"你好，我遇到了一个问题，就是我的代码在运行的时候会报错，
具体的错误信息我也不知道怎么描述，反正就是不太对劲，
你能不能帮我详细地检查一下我的代码，包括每一个函数，
每一个变量，每一个逻辑流程，然后告诉我到底哪里出错了，
最好还能给我一些改进建议..."
```

**Token-Saver 优化后**：
```
💡 Token 优化建议：
原始输入 (预估 180 tokens)：
  "你好，我遇到了一个问题，就是我的代码在运行的时候会报错..."
  
建议修改为 (预估 35 tokens)：
  "检查代码报错原因，提供修复建议"
  
预计节省：145 tokens (80%)
```

### 示例 2：移除冗余表达

| 原始输入 | 优化后 | 节省 |
|---------|--------|------|
| "请帮我详细地分析一下这个代码" | "分析代码" | 60% |
| "能不能帮我检查一下这个文件" | "检查文件" | 65% |
| "我想要一步一步地了解这个功能" | "解释功能" | 70% |

---

## 上下文管理示例

### 示例 1：对话过长提醒

**场景**：对话进行到 23 轮，token 消耗达 12,500

**Token-Saver 提醒**：
```
⚠️ 上下文管理提醒：
当前对话：23 轮，预估 12,500 tokens
建议操作：
  1. 使用 /compact 压缩历史记录
  2. 将关键信息保存到 memory：
     - 项目：/path/to/project
     - 问题：修复登录 bug
     - 进度：已完成 50%
  3. 开启新对话处理新任务
```

### 示例 2：自动总结关键信息

**用户请求**：`"帮我总结一下前面的讨论"`

**Token-Saver 返回**：
```
📊 最近 5 轮对话总结：

1. 用户问：如何修复登录 bug
2. AI 回答：检查验证码逻辑
3. 用户问：具体在哪一行
4. AI 回答：第 42 行
5. 用户问：怎么修改

💡 关键结论：
  - Bug 位置：login.py 第 42 行
  - 原因：验证码未正确验证
  - 解决方案：添加 validate_captcha() 调用
```

---

## 输出控制示例

### 示例 1：自动选择输出模式

| 用户请求 | 任务类型 | 输出模式 | 说明 |
|---------|---------|---------|------|
| "这个代码有 bug" | code_debug | detail | 详细输出错误信息 |
| "读取 config.json" | file_read | short | 只返回文件内容 |
| "解释什么是递归" | concept | normal | 中等详细程度 |
| "生成 Python 函数" | code_gen | short | 只返回代码 |

### 示例 2：手动切换模式

```
用户："@token-saver mode short"
AI： ✅ 已切换到简短输出模式

用户："@token-saver mode detail"
AI： ✅ 已切换到详细输出模式

用户："@token-saver mode auto"
AI： ✅ 已恢复到自动模式
```

---

## 文件读取优化示例

### 示例 1：使用缓存

**第 1 次读取**：
```
用户："读取 main.py"
AI： [读取文件并缓存]
💡 已缓存 main.py （3 轮内重复读取将使用缓存）
```

**第 2 次读取（2 轮内）**：
```
用户："再看一下 main.py"
AI： [使用缓存，不消耗 token]
♻️ 使用缓存内容（main.py）
```

### 示例 2：片段读取

**场景**：文件有 2000 行，用户只需要查看第 150 行附近

```
用户："查看 main.py 第 150 行"

AI：
💡 检测到大型文件（2000 行），建议阅读片段
📂 正在读取 main.py 第 140-160 行...

[只返回第 140-160 行的内容]

💰 节省：~1800 tokens (90%)
```

---

## 模式检测与脚本生成示例 ⭐

### 示例 1：重复的文件检查

**场景**：用户连续 3 次请求检查不同文件的语法

**用户重复请求**：
```
1. "检查 main.py 有没有语法错误"
2. "检查 utils.py 有没有语法错误"
3. "检查 config.py 有没有语法错误"
```

**Token-Saver 检测到模式**：
```
🤖 检测到重复操作模式！

我发现您在最近 10 轮对话中，重复执行了类似的操作 3 次：
  1. "检查 main.py 语法"
  2. "检查 utils.py 语法"
  3. "检查 config.py 语法"

💡 建议：
  我可以为您生成一个批量检查脚本，下次您只需要说：
  "运行语法检查" 即可完成所有操作。

📊 预期收益：
  - 每次节省：~600 tokens
  - 如果重复 10 次：节省 ~6,000 tokens

❓ 是否生成脚本？
  [是] 生成并保存到项目目录
  [否] 本次不生成
  [总是] 以后自动生成，无需询问
```

**用户选择"是"**：

**生成的脚本** (`batch_syntax_check.sh`)：
```bash
#!/bin/bash
# 自动生成的脚本 - 批量语法检查
# 触发原因：检测到重复的文件检查请求 (3 次)

files=("main.py" "utils.py" "config.py")

for file in "${files[@]}"; do
  echo "检查 $file ..."
  python3 -m py_compile "$file"
  if [ $? -eq 0 ]; then
    echo "✅ $file 语法正确"
  else
    echo "❌ $file 存在语法错误"
  fi
done

echo ""
echo "✅ 批量检查完成"
```

**优化后的交互**：
```
用户："运行语法检查"

AI：
🚀 执行脚本：batch_syntax_check.sh

检查 main.py ...
✅ main.py 语法正确

检查 utils.py ...
✅ utils.py 语法正确

检查 config.py ...
✅ config.py 语法正确

✅ 批量检查完成

💰 Token 节省统计：
  原始：3 次对话 × 600 tokens = 1,800 tokens
  脚本：1 次对话 × 200 tokens = 200 tokens
  节省：1,600 tokens (89%) ✓
```

---

### 示例 2：相似意图的查询

**场景**：用户连续 3 次查询不同股票的价格

**用户重复请求**：
```
1. "查询苹果股价"
2. "查询特斯拉股价"
3. "查询谷歌股价"
```

**Token-Saver 检测到模式**：
```
🤖 检测到相似的操作模式！

模式类型：相似意图 (similar_intent)
重复次数：3 次

💡 建议：
  我可以为您生成一个批量查询脚本，下次您只需要说：
  "查询股票" 即可查询所有股票。

是否生成脚本？[是/否/总是]
```

**用户选择"是"**：

**生成的脚本** (`batch_stock_query.py`)：
```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
# 自动生成的脚本 - 批量股票查询

import sys

# 默认股票列表
stocks = ["AAPL", "TSLA", "GOOGL"]

def query_stock_price(symbol):
    """查询股票价格（示例函数）"""
    # 这里调用实际的股价查询 API
    print(f"查询 {symbol} 的实时价格...")
    pass

if __name__ == "__main__":
    if len(sys.argv) > 1:
        # 用户指定了股票代码
        for symbol in sys.argv[1:]:
            query_stock_price(symbol)
    else:
        # 使用默认列表
        for symbol in stocks:
            query_stock_price(symbol)
        
    print("✅ 批量查询完成")
```

**优化后的交互**：
```
用户："查询股票"

AI：
🚀 执行脚本：batch_stock_query.py

查询 AAPL 的实时价格...
查询 TSLA 的实时价格...
查询 GOOGL 的实时价格...

✅ 批量查询完成

💰 节省：1,300 tokens (87%)
```

---

### 示例 3：重复的操作序列

**场景**：用户每次调试都要执行固定的操作序列

**用户重复操作**：
```
每次调试都要：
1. 读取错误日志 → Read /var/log/app.log
2. 检查配置 → Read /etc/app/config.yaml
3. 运行测试 → Bash pytest
4. 重启服务 → Bash systemctl restart app
```

**Token-Saver 检测到模式**：
```
🤖 检测到重复的操作序列！

序列指纹：a3f5b2c1...
重复次数：3 次

💡 建议：
  我可以为您生成一个自动化脚本，下次您只需要说：
  "运行调试流程" 即可完成所有操作。

是否生成脚本？[是/否/总是]
```

**生成的脚本** (`debug_workflow.sh`)：
```bash
#!/bin/bash
# 自动生成的脚本 - 标准调试流程
# 触发原因：检测到重复的操作序列 (3 次)

echo "=== 步骤 1: 读取错误日志 ==="
tail -n 50 /var/log/app.log

echo ""
echo "=== 步骤 2: 检查配置文件 ==="
cat /etc/app/config.yaml | grep -v "^#"

echo ""
echo "=== 步骤 3: 运行单元测试 ==="
python3 -m pytest tests/

echo ""
echo "=== 步骤 4: 重启服务 ==="
systemctl restart app.service

echo ""
echo "✅ 调试流程完成"
```

---

## 综合示例

### 示例：完整的 Token 节省流程

**场景**：用户需要进行一次代码审查

**第 1 步：输入优化**
```
用户："请帮我详细地分析一下这个代码文件的每一个部分，包括..."

AI：
💡 Token 优化建议：
  原始：120 tokens → 优化后：30 tokens
  节省：90 tokens (75%)
  
是否应用优化？[是/否]
```

**第 2 步：文件读取优化**
```
AI：
📂 检测到 3 个文件需要读取
💡 建议批量读取以节省 token

开始批量读取：
  ✅ 读取 main.py (250 行)
  ✅ 读取 utils.py (180 行)
  ✅ 读取 config.py (50 行)
  
💰 节省：~400 tokens (50%)
```

**第 3 步：输出控制**
```
AI：
📊 任务类型：代码审查
🎯 输出模式：normal (自动选择)

[返回代码审查结果，中等详细程度]
```

**第 4 步：模式检测（如果重复）**
```
（如果用户之前已经做过 3 次代码审查）

AI：
🤖 检测到重复模式！

建议生成脚本："运行代码审查"

是否生成？[是]
```

---

## 📊 效果对比

| 场景 | 优化前 Token | 优化后 Token | 节省 | 节省率 |
|------|-------------|-------------|------|--------|
| 冗长输入 | 500 | 100 | 400 | 80% |
| 重复文件读取 | 3000 | 300 | 2700 | 90% |
| 长对话未压缩 | 20000 | 5000 | 15000 | 75% |
| 重复操作（3次） | 4500 | 500 | 4000 | 89% |
| **综合** | **28000** | **5900** | **22100** | **79%** |

---

**更多示例？** 查看 [API.md](./API.md) 或提交 Issue！
